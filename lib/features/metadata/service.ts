import c_service from '@/lib/features/core/service'

export default class metadataService extends c_service {
  /**
   * constructor function for metadataService.
   *
   * @remarks
   * This method is part of the metadataService class extended of the main parent class baseService.
   *
   * @param model - metadataSchema
   *
   * @beta
   */
  constructor(model: any) {
    super(model)
  }
}
